import {fireEvent,render,screen,waitFor} from "@testing-library/react";import {beforeEach,describe,expect,it,vi} from "vitest";import {Workbench} from "./workbench";vi.mock("next/navigation",()=>({useSearchParams:()=>new URLSearchParams()}));describe("Workbench",()=>{beforeEach(()=>vi.stubGlobal("fetch",vi.fn().mockResolvedValue({ok:true,json:async()=>({games:[{appid:9000004,name:"TUNIC",release_year:2022,short_description:"Synthetic secrets",header_image_url:null,steam_url:"https://store.steampowered.com/app/9000004"}]})})));it("selects search result with keyboard",async()=>{render(<Workbench/>);const input=screen.getByRole("combobox");fireEvent.change(input,{target:{value:"tu"}});await waitFor(()=>expect(screen.getByRole("option")).toHaveTextContent("TUNIC"));fireEvent.keyDown(input,{key:"Enter"});expect(screen.getByLabelText("Selected seed games")).toHaveTextContent("TUNIC");expect(screen.getByLabelText("TUNIC seed weight")).toHaveValue("1")});it("focuses invalid seed",()=>{render(<Workbench/>);fireEvent.click(screen.getByRole("button",{name:/Find my next game/}));expect(screen.getByRole("status")).toHaveTextContent("Choose at least one seed");expect(screen.getByRole("combobox")).toHaveFocus()})});
